{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Go Program",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/coldstarter",
            "console": "internalConsole",
            "env": {
                "CONFIG": "${workspaceFolder}/configs/config.toml"
            }
        }
    ]
}